# Local development uses the active gcloud account; no secret is stored.
GOOGLE_GENAI_USE_VERTEXAI=true
GOOGLE_CLOUD_PROJECT=gen-lang-client-0140113557
GOOGLE_CLOUD_LOCATION=global
FORMBRIDGE_USE_GCLOUD_AUTH=true

# In Cloud Run, omit FORMBRIDGE_USE_GCLOUD_AUTH so runtime ADC is used.
